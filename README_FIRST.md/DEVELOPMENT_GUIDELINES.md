# LeadPilot Development Guidelines

Coding and style standards for developers.

---

## 1. Code Standards
* **HTML**: Always use semantic tags (`<section>`, `<article>`, `<header>`).
* **CSS Class Encapsulation**: Use prefixes for page features to avoid conflicts.
* **Vanilla JS Performance**: Wrap scroll event callbacks inside a passive listener to prevent page lag.
